apiVersion: v1
kind: Service
metadata:
  name: {{ include "nextjs-helm-chart.fullname" . }}
  labels:
    {{- include "nextjs-helm-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "nextjs-helm-chart.selectorLabels" . | nindent 4 }}
